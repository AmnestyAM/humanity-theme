name: SCSS Lint, Compile, and Package
on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
    - name: Enable Corepack
      run: corepack enable
    - name: Install dependencies
      run: cd private && yarn
    - name: Compile SCSS to CSS
      run: cd private && yarn build
    - name: Archive production artifacts
      run: zip -r ./AM-theme.zip ./wp-content/themes/humanity-theme
      # Adjust the paths and exclusions according to your project structure
    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: AM-theme-zip
        path: AM-theme.zip

# add POMO file support 
